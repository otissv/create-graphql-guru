'use strict';
import {
  MongoDBMutation
} from '../../../core/database/mongodb/mongodb-database';

export default class User extends MongoDBMutation {
  // create ({ args, req, databases, locals, models }) {
  //   console.log
  // }
}
